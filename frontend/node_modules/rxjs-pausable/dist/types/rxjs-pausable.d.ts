import { Observable } from 'rxjs';
export declare class PausableObservable<T> extends Observable<T> {
    private pauser;
    pause(): void;
    resume(): void;
}
export declare function pausable(): <T>(source: Observable<T>) => PausableObservable<T>;
